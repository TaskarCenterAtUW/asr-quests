---
description: Project-wide instructions for the ASR Quests workspace
applyTo: "**"
---

<!-- @format -->

IMPORTANT: This project uses a Python virtual environment (`.venv/`). ALWAYS run `.\.venv\Scripts\Activate.ps1` from the repo root before ANY terminal command (npm, pip, python, utility scripts, etc.). Never assume the venv is already activated.
